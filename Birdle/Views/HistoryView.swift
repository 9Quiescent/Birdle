import SwiftUI

struct HistoryView: View {
    var body: some View {
        List {
            Section {
                Text("Games are going to go here")
                    .foregroundColor(.secondary)
            }
        }
        .navigationTitle("History")
    }
}
